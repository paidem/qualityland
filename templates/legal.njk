{% extends "base.njk" %}
{% block title %}{{ page.title }}{% endblock %}
{% block content %}
<section class="legal">
  <div class="container">
    <h1>{{ page.title }}</h1>
    {{ page.content | safe }}
  </div>
</section>
{% endblock %}
